">
            <button type="submit" name="add_item">Post to Site</button>
        </form>
    </div>

    <h3>Manage Posted Content</h3>
    <table>
        <thead>
            <tr>
                <th>Title</th>
                <th>Category</th>
                <th>Price (KES)</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <?php
            $res = mysqli_query($conn, "SELECT * FROM packages ORDER BY id DESC");
            if (mysqli_num_rows($res) > 0) {
                while($row = mysqli_fetch_assoc($res)) {
                    echo "<tr>";
                    echo "<td>" . htmlspecialchars($row['title']) . "</td>";
                    echo "<td>" . $row['category'] . "</td>";
                    echo "<td>" . number_format($row['price']) . "</td>";
                    echo "<td>
                            <a href='edit.php?id=" . $row['id'] . "' class='btn-edit'>Edit</a>
                            <a href='admin_manage.php?delete=" . $row['id'] . "' class='btn-delete' onclick='return confirm(\"Are you sure you want to delete this destination?\")'>Delete</a>
                          </td>";
                    echo "</tr>";
                }
            } else {
                echo "<tr><td colspan='4'>No packages found. Add one above!</td></tr>";
            }
            ?>
        </tbody>
    </table>
</body>
</html>
